import React from 'react';
import './ProjectPage.css'

const ProjectPage = () => {
    return (
        <div className={'ProjectPage'}>
            <h2>Projects</h2>
            <a href={'https://jakubcendrowski.pl/wave-function-collapse-algorithm/'}>wave function collapse algorithm (work in progress)</a>
        </div>
    );
};

export default ProjectPage;